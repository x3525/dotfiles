include config.d/*.conf

# Screen locker
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/.local/bin/C2 LOCK

# Executing applications
exec_always --no-startup-id ~/.local/bin/C2 RESTART ~/.config/autostart/desktop.d

focus_follows_mouse no

focus_on_window_activation urgent
